interface:
  display_name: "VS Code Profiles Manager"
  short_description: "Create and directly manage VS Code profiles"
  default_prompt: "Use $vscode-profiles-manager to manage my VS Code profiles from what I ask for."
policy:
  allow_implicit_invocation: true
